# Copy this file to .env and replace every value with your local settings.
# Spring Boot does not load .env automatically; export these variables in your shell
# or configure them in your IDE's run configuration.
DB_URL=jdbc:postgresql://localhost:5432/restaurant_db
DB_USERNAME=postgres
DB_PASSWORD=change-me
ADMIN_USERNAME=restaurant-admin
ADMIN_PASSWORD=change-me-too
